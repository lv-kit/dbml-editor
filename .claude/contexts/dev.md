# context: dev（実装モード）

通常の実装作業のモード。

## 姿勢
- 標準エフォート・Sonnet。単純編集は Haiku、横断/アーキ判断のみ Opus
- 標準フロー: Understand → Plan → TDD → Review → CI → Commit → PR（`.claude/rules/common/development-workflow.md`）

## よく使う
- `/plan` `/tdd` `/ci-all` `/code-review` `/pr`
- 検索は semble 第一選択、Svelte は Svelte MCP
